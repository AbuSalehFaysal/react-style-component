import React from 'react';
import styles from './Footer.module.css'

const Footer = () => {
    return (
        <div>
            <p className={styles.title}><small>FOOTER COMPONENT</small></p>
        </div>
    );
};

export default Footer;